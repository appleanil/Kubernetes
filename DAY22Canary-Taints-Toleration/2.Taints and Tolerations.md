# Taints and Tolerations in Kubernetes

Taints anedi Nodes medha restrict cheyali 

for ex: Two nodes unnai Node 1 and Node 2

Case: 01 Node 1 ki nenu taints cheysanu with some conaditions like key=value:NoSchedule so Pods kuda aa condition statisfy ithy aa node lo deploy avthundi leduante 
vere node lo deploy avthundi. And Node2 లో కూడా అవచ్చు (scheduler decision)

❌ Case 02: Pod కి toleration లేదు

Node1 → ❌ NOT allowed
Node2 → ✅ schedule అవుతుంది

Important Edge Case (VERY IMPORTANT)

If only Node1 exists (tainted node only):

Pod కి toleration లేకపోతే
❌ Pod Pending లో ఉంటుంది
Node2 కి వెళ్లదు (because Node2 లేదు)

Tolerations anedi Pods yaml file write cheyvachu

| Pod                | Node1 (tainted) | Node2 (normal) |
| ------------------ | --------------- | -------------- |
| With toleration    | ✅               | ✅              |
| Without toleration | ❌               | ✅              |


Taints and tolerations are used to control pod scheduling by restricting which pods can run on specific nodes, ensuring workload isolation and better resource management.

🔥 1️⃣ NoSchedule
Meaning:

కొత్త Pods ఆ node మీద schedule అవ్వవు.

అంటే pod కి matching toleration లేకపోతే ఆ node లో deploy కాదు.

Example:
kubectl taint nodes node1 db=internal:NoSchedule
Telugu:

👉 node1 మీద db pods మాత్రమే రావాలి
👉 normal pods అక్కడ deploy కావు

Real Time:

DB server node reserve చేయాలి.

🔥 2️⃣ PreferNoSchedule
Meaning:

Kubernetes try చేస్తుంది ఆ node మీద pod deploy చేయకుండా.

కానీ అవసరం అయితే deploy చేస్తుంది.

Example:
kubectl taint nodes node2 gpu=true:PreferNoSchedule
Telugu:

👉 possible అయితే ఇతర nodes లో deploy చేస్తుంది
👉 cluster full అయితే ఈ node లో కూడా deploy చేస్తుంది

Real Time:

GPU node ని normal apps avoid చేయాలి.

🔥 3️⃣ NoExecute
Meaning:

కొత్త pods schedule కావు + already running pods కూడా remove (evict) అవుతాయి.

Example:
kubectl taint nodes node3 maintenance=true:NoExecute
Telugu:

👉 కొత్త pods రావు
👉 already running pods బయటకు పంపబడతాయి

Real Time:

Node maintenance mode లో పెట్టినప్పుడు.

🔥 Easy Table
Type	Telugu Meaning
NoSchedule	కొత్త pods deploy అవ్వవు
PreferNoSchedule	deploy చేయకుండా try చేస్తుంది
NoExecute	deploy కాదు + running pods తొలగిస్తాయి
🔥 Real Time Example
Node 1 DB Server
db=internal:NoSchedule

DB pods మాత్రమే

Node 2 GPU Server
gpu=true:PreferNoSchedule

normal pods avoid చేస్తాయి

Node 3 Maintenance
maintenance=true:NoExecute

Pods remove అవుతాయి

🔥 Simple Telugu Memory Trick
NoSchedule = రానివ్వదు
PreferNoSchedule = రాకుండా చూస్తుంది
NoExecute = పంపేస్తుంది
🔥 Interview Style Answer

NoSchedule → New pods block
PreferNoSchedule → Avoid scheduling
NoExecute → Existing pods evict + new pods block

## Concept:
Taints and tolerations help control where pods can run in Kubernetes.

- **Taint:** Applied on a Node to repel pods from it.
- **Toleration:** Added in a Pod to allow it to be scheduled on a tainted Node.

## Simple Explanation:
Taints act like a "No Entry" sign on a Node.
Only Pods with a matching toleration (entry pass) can run on those Nodes.

## Why We Use Taints & Tolerations:
- Reserve Nodes for specific workloads like databases or GPU-heavy applications.
- Prevent normal application pods from using those Nodes.
- Achieve better performance, isolation, and resource management.

## Before Applying Taints:
Kubernetes schedules pods automatically on any available Node without restrictions.

### Example Pod (Without Toleration):
```yaml
apiVersion: v1
kind: Pod
metadata:
  name: normal-pod
spec:
  containers:
  - name: nginx
    image: nginx
````

This Pod can run on any Node.

## Applying Taint on Node:

Command to taint a Node:

```bash
kubectl taint nodes <node1> key=value:NoSchedule

kubectl taint nodes <node1> key=value:NoSchedule-    #untaints
```

Now, node1 will reject all pods unless they have a matching toleration.

## What Happens Now:

Pods without tolerations cannot run on the tainted Node and will remain in Pending state.

## Pod with Matching Toleration:

Example Pod YAML with toleration:

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: toleration-pod
spec:
  tolerations:
  - key: "key"
    operator: "Equal"
    value: "value"
    effect: "NoSchedule"
  containers:
  - name: nginx
    image: nginx
```

This pod can tolerate the taint and will be scheduled on node1.

## Practical Use Case Example:

### Scenario:

Dedicated database Nodes.

1. Apply Taint on Node:

```bash
kubectl taint nodes db-node db=exclusive:NoSchedule
```

2. Add Toleration to Database Pods:

```yaml
tolerations:
- key: "db"
  operator: "Equal"
  value: "exclusive"
  effect: "NoSchedule"
```

* Normal Pods will be blocked.
* Only Database Pods will run on db-node.

## Types of Effects in Taints:

| Effect           | Description                                                          |
| ---------------- | -------------------------------------------------------------------- |
| NoSchedule       | Blocks Pods without matching toleration from being scheduled.        |
| PreferNoSchedule | Tries to avoid scheduling Pods without toleration, but not enforced. |
| NoExecute        | Evicts running Pods without toleration and blocks future scheduling. |

## How to Remove Taint:

```bash
kubectl taint nodes node1 key=value:NoSchedule-
```

The Node becomes available for all Pods again.

## Key Summary:

* Taints restrict workloads on Nodes.
* Tolerations allow specific Pods to bypass those restrictions.
* Commonly used for dedicated Nodes (e.g., DB, GPU workloads).

## Learning Flow for Students:

1. Deploy a Pod normally; it runs anywhere.
2. Apply Taint; show Pods can’t run on that Node.
3. Add Toleration; show Pod can now run on tainted Node.
4. Remove Taint; Node returns to normal.
5. Discuss real-world use cases.

## Common Commands:

| Task              | Command Example                                  |
| ----------------- | ------------------------------------------------ |
| Add Taint         | kubectl taint nodes node1 key=value\:NoSchedule  |
| Remove Taint      | kubectl taint nodes node1 key=value\:NoSchedule- |
| Check Node Taints | kubectl describe node node1                      |
| Deploy Pod        | kubectl apply -f pod.yaml                        |




