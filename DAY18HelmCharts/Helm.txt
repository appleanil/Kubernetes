USEFUL IN SELF MANAGED K8S CLUSTER ONLY NOT IN EKS K8S CLUSTER

Helm in k8s
===========
Helm is a package manager for Kubernetes applications that helps you define, install, and manage Kubernetes applications similar to yum/apt for linux distributions. 


It uses a concept called "charts" to package all the resources(pod,rs,deploy,pv,pvc.....) needed for an application into a single unit.
Here’s a detailed breakdown of Helm and its components:


1. Key Concepts
---------------
Charts: A Helm chart is a collection of files that describe a related set of Kubernetes resources. Each chart contains templates for Kubernetes manifests, 
        a Chart.yaml file (metadata), and optionally a values.yaml file for configuration.

Releases: A release is an instance of a chart running in a Kubernetes cluster. When you install a chart, you create a release. Each release can have multiple versions, 
          and you can upgrade or roll back to previous versions.

Repository: Helm charts can be stored in repositories, which are collections of charts. You can create your own chart repositories or
            use public ones like the official Helm Hub or Artifact Hub.


2. Architecture
----------------
Helm operates in a client-server model:

helm 2:

https://www.google.com/search?q=helm+in+k8s+2&sca_esv=9c1db43462cf1df4&rlz=1C1UEAD_enIN1101IN1102&udm=2&biw=1440&bih=825&sxsrf=ADLYWIK4Xt_7ap01lm4VoujMDzvZC5TY6Q%3A17289024
69006&ei=RfUMZ5ILsZ7j4Q__5ILIAg&ved=0ahUKEwjSwZDM142JAxUxzzgGHXyACkQ4dUDCBA&uact=5&oq=helm+in+k8s+2&gs_lp=Egxnd3Mtd2l6LXNlcnAiDWhlbG0gaW4gazhzIDJIpQ5Q7AJYkgxwAXgAkAEAmAFhoA
GRBaoBATe4AQPIAQD4AQGYAgWgAq8DwgIKEAAYgAQYQxiKBcICBRAAGIAEwgIEECMYJ8ICBhAAGAcYHsICCBAAGAUYBxgewgIIEAAYBxgIGB7CAgYQABgFGB7CAgYQABgIGB6YAwCIBgGSBwMyLjOgB_wd&sclient=gwswizser
#vhid=CnN5ezLxGwlb8M&vssid=mosaic

helm 3:

https://www.google.com/search?q=helm+in+k8s+3+archetecture&sca_esv=9c1db43462cf1df4&rlz=1C1UEAD_enIN1101IN1102&udm=2&biw=1440&bih=825&sxsrf=ADLYWILeu84H6o3S6g8w1f-
ITRaOqfA84Q%3A1728902522703&ei=evUMZ9jNKsKW4EPmq6auQs&ved=0ahUKEwjY8t3l142JAxVCyzgGHRqXJrcQ4dUDCBA&uact=5&oq=helm+in+k8s+3+archetecture&gs_lp=Egxnd3Mtd2l6LXNlcnAiG
mhlbG0gaW4gazhzIDMgYXJjaGV0ZWN0dXJlSJ8lUOEBWPojcAF4AJABAJgB0QGgAZkMqgEFNS43LjG4AQPIAQD4AQGYAgCgAgCYAwCIBgGSBwCgB8kE&sclient=gws-wiz-serp#vhid=HShMz9xvTEjAwM&vssid=mosaic


3. Installation
----------------
curl -fsSL -o get_helm.sh https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3

chmod 700 get_helm.sh

sh get_helm.sh

4. How to uninstall helm
-------------------------
sudo rm /usr/local/bin/helm

rm -rf ~/.config/helm

rm get_helm.sh

5. Chart Structure
------------------
IQ. Plz explain helm chart structure

Helm chart is a package of Kubernetes resources. It contains Chart.yaml for metadata, values.yaml for configuration, templates folder for Kubernetes manifests,
and charts folder for dependencies.


A typical Helm chart structure looks like this:

jio-stg-om-chart/
  mychart/
├── Chart.yaml          # Metadata about the chart (or) Info about project apiVersion, Name, Description, Version, appVersion
├── values.yaml         # Default configuration values hardcode cheykunda undali ante values.yaml lo paste cheyali like app, env, name, replicas, secrets, ports, image
├── charts/             # Subcharts (if any) (or) Dependencies
└── templates/          # Kubernetes resource templates (or) Kubernetes YAML
    ├── deployment.yaml
    ├── service.yaml
    ├── pv.yaml
    ├── pvc.yaml
    ├── configmap.yaml
    ├── secret.yaml
    ├── ingress.yaml
    ├── hpa.yaml
    ├── serviceaccount.yaml
    ├── role.yaml
    ├── rolebinding.yaml
    ├── statefulset.yaml
    ├── daemonset.yaml
    ├── job.yaml
    ├── cronjob.yaml
    ├── networkpolicy.yaml
    ├── poddisruptionbudget.yaml
    ├── resourcequota.yaml
    ├── limitrange.yaml

Example for deployment
----------------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .values.namespace }}     #we can define this as values.yaml
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}
    spec:
      containers:
      - name: {{ .Release.Name }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        ports:
        - containerPort: {{ .Values.service.targetPort }}


6. Templating
--------------
Helm uses Go templates to generate Kubernetes manifests dynamically. You can use placeholders in your templates, which get replaced by values from values.yaml or
from command-line parameters when deploying.


7. Values.yaml 
---------------
You can customize deployments by creating your own values.yaml file, overriding default values in the chart:

stgvalues.yaml
--------------
replicaCount: 2
image:
  repository: my-repo/my-app
  tag: "1.0.0"
service:
  type: LoadBalancer
  port: 80
request:
 cpu: 1
 memory: 12Gi


prod-values.yaml
----------------
replicaCount: 5
image:
  repository: my-repo/my-app-prod
  tag: "1.1.0"
service:
  type: LoadBalancer
  port: 80
request:
 cpu: 8
 memory: 128Gi



8. Best Practices
-----------------
Version Control: Store your charts in a version control system.
Reusable Charts: Create charts that can be reused across different environments.
Testing: Use the helm test command to verify chart functionality.
Security: Keep your charts and values secure, especially sensitive data.


9. Common Charts
-----------------
Many common applications have Helm charts available, including:

NGINX
Prometheus
Grafana
MySQL
PostgreSQL
metrics-server
jenkins

You can find these charts in public repositories like the Helm Hub.

==============================================================================================================================================

kubectl config current-context  --> to check the correct environment
kubectl config get-context      --> shows all clusters env
kubectl top nodes
kubectl top pods

USEFUL IN SELF MANAGED K8S CLUSTER ONLY NOT IN EKS K8S CLUSTER

How to deploy third party applications like metrics server, prometheus, etc ?

EX: metrics server, serach in aritfacthub website --> https://artifacthub.io/

search metrics-server

url: https://artifacthub.io/packages/helm/metrics-server/metrics-server

How to check helm repo added or not?

helm repo ls

How to add helm repo?

helm repo add metrics-server https://kubernetes-sigs.github.io/metrics-server/




helm repo ls

How to search chart for that repo? ---------------> helm search repo metrics-server

How to see the default values of that chart? -----> helm show values metrics-server/metrics-server

How to see the template(all yaml files) ? --------> helm template metrics-server/metrics-server


===================================================================================================================================================


How to install/deploy the chart?

helm upgrade --install metrics-server metrics-server/metrics-server -n test-ns --dry-run

helm upgrade --install metrics-server metrics-server/metrics-server --set replicas=2 -n test-ns --dry-run

helm upgrade --install metrics-server metrics-server/metrics-server -n test-ns --dry-run

helm upgrade --install metrics-server metrics-server/metrics-server -n test-ns

helm ls -A

helm ls -n kube-system



Increased to 2 replicas
-----------------------
helm upgrade --install metrics-server metrics-server/metrics-server --set replicas=3


How to rollback
----------------
helm ls
helm rollback metrics-server ------> previous version


How to uninstall
----------------
helm uninstall metrics-server -----> appplication will delete, not helm uninstall


=================================================================================================================================================

NOTE: If you want to pass more values through --set is not a good practise, so we need to use values.yaml

helm show values metrics-server/metrics-server

vi metricservervalues.yaml

resources:
  requests:
    cpu: 300m
    memory: 512Mi
replicas: 2


helm upgrade --install metrics-server -f metricservervalues.yaml metrics-server/metrics-server

kubectl describe po <pod-name> --> to see the updated values


--> create a production cluster separate values.yaml and deploy


===============================================================================================================================================

To deploy your Kubernetes resources as a Helm chart, you'll need to create a basic Helm chart structure.
Here’s a step-by-step guide to help you set up your Helm chart with the provided Deployment and Service configurations.
IQ.] Where is ur all yaml files
ans: templates section

Step 1: Create a Helm Chart
---------------------------
First, create a new Helm chart:

helm create javawebapp ----------> to create one folder name with javawebapp

yum install tree -y 

This will create a directory structure like this:

javawebapp/ (or) ordermanagement (or) billing like this
├── Chart.yaml
├── templates/
│   ├── deployment.yaml
│   ├── service.yaml
│   └── PVC.yaml
└── values.yaml



Step 2: Modify the Chart.yaml
-----------------------------
Edit the Chart.yaml file to provide metadata for your chart. Here’s an example:

apiVersion: v2
name: javawebapp
description: A Helm chart for deploying a Java web application
version: 1.0.0          #helm version
appVersion: "1.0.1"     #image version means application version


Step 3: Update the values.yaml
------------------------------
In values.yaml, set default values that can be customized when deploying. For example:

replicaCount: 2

image:
  repository: kkeducation123456/java-web-app
  pullPolicy: IfNotPresent
  tag: "1.0.1"          #image version means application version

service:
  type: NodePort
  port: 80
  targetPort: 8080

namespace: prod


Step 4: Edit the Deployment Template
------------------------------------
In templates/deployment.yaml, replace its content with your Deployment configuration, making use of Helm templates:

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Values.namespace }}     #we can define this as values.yaml
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}
    spec:
      containers:
      - name: {{ .Release.Name }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        ports:
        - containerPort: {{ .Values.service.targetPort }}


Step 5: Edit the Service Template
---------------------------------
In templates/service.yaml, modify it to use values from values.yaml:

apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ .Release.Name }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}


Step 6: Deploy the Helm Chart
-----------------------------
Now that you have created your Helm chart, you can install it using the following command:

"DO IN ROOT"
BEFORE RUUNING BELOW COMMAND PLZ ADD --dry-run to check ----------> helm upgrade --install javawebapp ./javawebapp  --dry-run

helm install <release-name> ./javawebapp

1. helm install javawebapp ./javawebapp

NAME: javawebapp
LAST DEPLOYED: Tue Mar 31 12:12:12 2026
NAMESPACE: default
STATUS: deployed
REVISION: 1
TEST SUITE: None

2. helm upgrade --install javawebapp ./javawebapp  --dry-run ----> show all yaml files without hardcoded

3. helm upgrade --install javawebapp ./javawebapp

Release "javawebapp" has been upgraded. Happy Helming!
NAME: javawebapp
LAST DEPLOYED: Tue Mar 31 12:13:57 2026
NAMESPACE: default
STATUS: deployed
REVISION: 2
TEST SUITE: None

4. helm ls ---------> helm chart version
                ---------Revision----------
NAME         NAMESPACE   REVISION   UPDATED                              STATUS     CHART              APP VERSION
javawebapp   default     1          2025-09-08 02:04:33.886808897 +0000   deployed   javawebapp-1.0.0   1.0.1

5. helm upgrade --install javawebapp --set replicacount=3

6. helm ls
                ---------Revision----------
NAME         NAMESPACE   REVISION   UPDATED                              STATUS     CHART              APP VERSION
javawebapp   default     2          2025-09-08 02:04:33.886808897 +0000   deployed   javawebapp-1.0.0   1.0.1

7. helm rollback javawebapp --------> previous version

8. helm ls 

                ---------Revision----------
NAME         NAMESPACE   REVISION   UPDATED                              STATUS     CHART              APP VERSION
javawebapp   default     3          2025-09-08 02:04:33.886808897 +0000   deployed   javawebapp-1.0.0   1.0.1
================================================================================================================================================

1. helm ls -n prod

2. helm create <chartName>

3. helm lint <chartName> --------> for validation
==> Linting javawebapp
[INFO] Chart.yaml: icon is recommended

1 chart(s) linted, 0 chart(s) failed


4. helm package javawebapp  -----> to create a tar file

5. helm upgrade --install javawebapp2 javawebapp-1.0.0.tgz -n prod --------> by this tar file also we can deploy "NEW" helm charts in "PROD ns
   helm upgrade --install javawebapp2 javawebapp-1.0.0.tgz ----------------> default namespace
   helm ls -n prod
   kubectl get all -n prod and kubectl get all -n default

6. helm show values javawebapp-1.0.0.tgz --------> to display the values.yaml

helm template ./javawebapp > output.yaml
kubectl apply -f output.yaml

helm status <resourceName> --show-resources
helm status javawebapp --show-resources
|
|
NAME: javawebapp
LAST DEPLOYED: Tue Mar 31 12:15:00 2026
NAMESPACE: default
STATUS: deployed
REVISION: 3
TEST SUITE: None

RESOURCES:
==> v1/Service
NAME         TYPE       CLUSTER-IP     PORT(S)
javawebapp   NodePort   10.96.10.10    80:30007/TCP

==> apps/v1/Deployment
NAME         READY   UP-TO-DATE   AVAILABLE
javawebapp   2/2     2            2

helm uninstall mavenwebapp -n <NS> ---------> delete the folder/directory
helm ls

well practise with help of chtgpt all yaml files

======================================================================================================================================================

1. Basic Helm Commands
----------------------
helm version
helm help
helm repo list
helm repo add bitnami https://charts.bitnami.com/bitnami
helm repo update
helm search repo nginx
helm search hub nginx

2. Working with Charts
-----------------------
helm create mychart
helm lint mychart
helm package mychart
helm show chart mychart
helm show values mychart
helm show all mychart

3. Install / Upgrade / Uninstall
---------------------------------
helm install myapp ./mychart
helm install myapp bitnami/nginx
helm install myapp ./mychart -f values.yaml
helm install myapp ./mychart --set replicaCount=3

helm upgrade myapp ./mychart
helm upgrade myapp ./mychart -f values.yaml
helm upgrade myapp ./mychart --set image.tag=2.0

helm uninstall myapp

4. Release Management
---------------------
helm list
helm list -A
helm list -n default

helm status myapp
helm history myapp

5. Rollback & Debugging
-----------------------
helm rollback myapp 1
helm rollback myapp 2

helm get all myapp
helm get values myapp
helm get manifest myapp

helm install myapp ./mychart --dry-run
helm install myapp ./mychart --debug

6. Advanced Commands
--------------------
helm template myapp ./mychart
helm template myapp ./mychart -f values.yaml

helm dependency update
helm dependency build

helm upgrade --install myapp ./mychart

helm install myapp ./mychart --namespace dev --create-namespace

helm upgrade myapp ./mychart --reuse-values

helm uninstall myapp --keep-history

7. Working with Values
----------------------
helm install myapp ./mychart -f custom-values.yaml
helm install myapp ./mychart --set image.tag=latest
helm install myapp ./mychart --set replicaCount=3
helm upgrade myapp ./mychart --set service.type=NodePort

8. Testing & Validation
------------------------
helm lint mychart
helm template myapp ./mychart
helm install myapp ./mychart --dry-run --debug
